#ifndef CR_UTILS_H
#define CR_UTILS_H

// Standard Headers
#include <iostream>

// Logs a Message to the Console
void log_message(const char *message)
{
    std::cout << message << std::endl;
}

#endif // CR_UTILS_H
